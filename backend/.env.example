# ─────────────────────────────────────────────────────────────────────────────
# EvComms Backend — Environment Variables Reference
# Copy to .env for local development.
# Set in your host dashboard (Koyeb / Railway / Fly.io) for production.
# ─────────────────────────────────────────────────────────────────────────────

# ── Server ────────────────────────────────────────────────────────────────────
NODE_ENV=development           # development | production | test
PORT=3000
HOST=0.0.0.0

# ── App ───────────────────────────────────────────────────────────────────────
APP_NAME=EvComms
APP_URL=http://localhost:5173  # Production: https://yourdomain.com
CORS_ORIGIN=http://localhost:5173  # Production: https://yourdomain.com

# ── Database (Turso / libSQL) ─────────────────────────────────────────────────
# DEV  — local SQLite file (no auth token needed)
TURSO_DATABASE_URL=file:./local.db

# PROD — remote Turso cloud (uncomment and fill in)
# TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
# TURSO_AUTH_TOKEN=your-turso-auth-token

# OPTIONAL — embedded replica (local file + cloud sync):
# TURSO_DATABASE_URL=file:./local.db
# TURSO_SYNC_URL=libsql://your-db-name-your-org.turso.io
# TURSO_AUTH_TOKEN=your-turso-auth-token
# TURSO_LOCAL_DB_PATH=./local.db

# ── JWT ───────────────────────────────────────────────────────────────────────
# Generate: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Min 24 chars dev | min 32 chars production
JWT_SECRET=change-me-generate-with-crypto-randomBytes-48
JWT_EXPIRY_DAYS=30
# Required in production:
# JWT_ISSUER=https://yourdomain.com
# JWT_AUDIENCE=EvComms

# ── Cloudflare R2 (Media Storage) — required in production ────────────────────
# R2_ACCOUNT_ID=your-cloudflare-account-id
# R2_ACCESS_KEY_ID=your-r2-access-key-id
# R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
# R2_BUCKET_NAME=your-bucket-name
# R2_PUBLIC_URL=https://your-r2-public-url.r2.dev

# ── ImageKit (Media Upload / CDN) — optional ──────────────────────────────────
# IMAGEKIT_PUBLIC_KEY=your-imagekit-public-key
# IMAGEKIT_PRIVATE_KEY=your-imagekit-private-key
# IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your-id

# ── Email (at least one required in production) ───────────────────────────────

# Option A: Brevo (recommended)
# BREVO_API_KEY=your-brevo-api-key
# BREVO_SENDER_EMAIL=noreply@yourdomain.com
# BREVO_SENDER_NAME=EvComms

# Option B: Resend
# RESEND_API_KEY=your-resend-api-key

# Option C: SMTP
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SENDER_EMAIL=noreply@yourdomain.com
